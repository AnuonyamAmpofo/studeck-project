/// Flip to true if the backend/Supabase is ever unreachable (e.g. mid-
/// presentation) and you need the app click-through demoable with seeded
/// in-memory data instead. False by default now that the real backend
/// works end-to-end.
const bool kDemoMode = false;
